Разница между tx.Exec и tx.QueryRow в Go (PostgreSQL, database/sql)
Функция	         |   Когда использовать	                                                                    |  Возвращает
tx.Exec	         |   Когда не нужно получать данные (например, INSERT, UPDATE, DELETE без RETURNING)	    |   sql.Result (количество затронутых строк)
tx.QueryRow	      |  Когда нужно получить одну строку данных (например, SELECT или INSERT ... RETURNING id) |	*sql.Row (одна строка)


; тек 1 жолдың шыққанын тексермейді
tx.QueryRow	
QueryRow(query string, args ...any) *sql.Row
;  Если запрос вернет более одной строки, QueryRow все равно возьмет только первую.returning SUPPORT
; 🔹 Если строк нет, получим ошибку sql.ErrNoRows.
 tx.Get
 Get(dest interface{}, query string, args ...interface{}) error

🔹 tx.Exec 
Exec(query string, args ...any) (sql.Result, error)
; не может обработать результат SELECT, потому что он ожидает только выполнение команд (например, INSERT, UPDATE, DELETE).

; sqlx те бар,в аргументтер орнына берсе болады
tx.NamedExec
NamedExec(query string, arg any) (sql.Result, error)

tx.Query
Query(query string, args ...any) (*sql.Rows, error)

NamedQuery(query string, arg interface{}) (*sqlx.Rows, error)




; Если запрос возвращает строки, используйте Query
; for only one row, even it will be error if got 0 row
db.Get(&struct, Query, args...)  

; for get 0 or more rows
db.Select(&struct, query)



; PrepareNamed зачем если есть NamedExec
; ChatGPT said:
Хороший вопрос! PrepareNamed и NamedExec оба используются для работы с именованными параметрами в sqlx, но у них разные цели.

🔹 Разница между PrepareNamed и NamedExec

Функция	Когда использовать	                                                В чем разница?

NamedExec	Когда запрос выполняется один раз	                        Сразу подставляет параметры и выполняет запрос
PrepareNamed	Когда запрос будет выполняться много раз	            Создает подготовленный запрос, который можно использовать многократно




; COALESCE в SQL — это функция, которая возвращает первое ненулевое (не NULL) значение из списка аргументов.
; Синтаксис:

COALESCE(value1, value2, ..., valueN)

Она перебирает значения слева направо и возвращает первое, которое не NULL.
SELECT COALESCE(NULL, NULL, 'hello', 'world');  -- вернёт 'hello'
SELECT COALESCE(name, 'Unknown') FROM users;


; to_tsvector сам внутри делает всё в нижнем регистре.
; to_tsquery($1) тоже работает в нижнем регистре!


ts_rank()
; to_tsquery

; plainto_tsquery всегда использует оператор AND между словами и не поддерживает логические операторы, такие как | (OR).


ts_rank возвращает тип real (float4).











Что можно сделать лучше?

Если хочешь использовать индексы для скорости и рассчитывать ts_rank быстро, тогда есть такой приём:

✅ Создавать отдельное поле типа tsvector в таблице, например:

ALTER TABLE inventory ADD COLUMN search_vector tsvector;

Потом обновить его:

UPDATE inventory SET search_vector = 
    setweight(to_tsvector('english', name), 'A') || 
    setweight(to_tsvector('english', description), 'B');

Создать индекс:

CREATE INDEX idx_inventory_search_vector ON inventory USING GIN (search_vector);

И теперь твой запрос будет красивый и быстрый:

SELECT id, name, description, ...
FROM inventory
WHERE search_vector @@ to_tsquery($1)
ORDER BY ts_rank(search_vector, to_tsquery($1)) DESC;

Тут уже будет:

    и фильтрация через индекс

    и ts_rank по готовому tsvector



/*
	   SELECT
	       m.id AS id,
	       m.name,
	       m.description,
	       m.price,
	       ts_rank(to_tsvector(m.name || ' ' || m.description), plainto_tsquery('chocolate | cake')) AS relevance
	   FROM
	       inventory m
	   WHERE
	       to_tsvector(m.name || ' ' || m.description) @@ plainto_tsquery('chocolate | cake')

*/
